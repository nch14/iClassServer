package com.chenh.iClassServer.rest.controller;

import org.springframework.web.bind.annotation.*;

/**
 * Created by ChenhaoNee on 2016/11/26.
 */
@RestController
@RequestMapping(value = "/v1/classroom")
public class ClassController {

   /* @ApiOperation(value = "获取教室信息", notes = "")
    @ApiImplicitParam(name = "id", value = "用户的id", required = true, dataType = "Long")

    @RequestMapping(method = RequestMethod.GET,
            consumes = {"application/json", "application/xml"},
            produces = {"text/html", "application/xml"})
   *//* public User getUser(@RequestParam(value = "type", defaultValue = "233") String type,
                        HttpServletRequest request, HttpServletResponse response) {
        // 处理"/users/{id}"的GET请求，用来获取url中id值的User信息
        // url中的id可通过@PathVariable绑定到函数的参数中
        response.setStatus(200);
        return new User();*//*
    }*/
}
